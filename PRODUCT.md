# Product

## Register

product

> 依据：2026-09-08 本地源码复核。Capability Layers 描述现有能力；Product Purpose、Product Principles 和 Design Principles 是产品目标与设计要求。

## Product Definition

抓虾是面向电商运营的本地优先执行与 AI 内容生产平台。它把已登录浏览器、本地文件、企业网盘、AI/OCR、可复用 Adapter、人工审批和受控任务机连接成可执行、可恢复、可核验的业务工作流。

产品主路径不是单一的“网页抓取”，也不是单一的“云端审批”：

```text
确定目标 → 准备数据与素材 → 自动化 / AI 执行 → 按工作流审批 → 平台交付 → 结果读回
```

## Product Family

- **抓虾主项目**：确定性桌面工作台与平台主发布线。提供脚本/任务、AI 生图、生视频、Prompt、文件、浏览器和云端协作入口。
- **Crawshrimp Harness**：独立发布的智能体交互线。以 DSH 会话为主界面，通过 MCP 调用其自有后端中的 Task Instance、Adapter、浏览器、文件、媒体、审批和审计能力；能力代码按需从主项目移植。
- **云端审批台**：可选的多人协作模块。负责批次、Prompt、审批、RBAC、审计和任务机调度；它是抓虾的一部分，不代表整个产品。
- **Adapter 生态**：把平台能力组合成可安装的业务能力包。简单包可以只做网页自动化；复杂包可以协同 JS、Python、AI/OCR、PDF、云盘和验收测试。

## Users

- 电商/店铺运营：批量抓取、上传、发布、数据回收和日常巡检。
- 素材与内容团队：AI 生图、生视频、图片加工、Prompt 管理和图包交付。
- 审核与业务负责人：批次审图、重跑、通过/舍弃、提交任务机和查看结果。
- 技术型运营与开发者：开发 Adapter、维护企业集成、处理异常和建立验收规则。
- 管理员与任务机维护人员：配置权限、Provider、任务机、审计和部署。

## Product Purpose

让电商业务从“找数据/素材”到“生成、审核、上传、读回”的全链路在同一套受控系统中完成，减少手工搬运、线下表格流转和无法复现的个人脚本。

产品希望达到的成功状态是（恢复、审批与读回覆盖仍取决于具体工作流）：

1. 用户可以直接从表单、固定工作流或 Harness 对话发起任务；
2. 系统清楚展示正在使用的账号、文件、模型、能力和审批边界；
3. 中断或失败后可以重试、恢复或转人工，而不是从头重来；
4. 产物与平台写入都有读回证据，业务结果可核验；
5. 企业定制被隔离在 Adapter/配置中，公共底座保持可复用。

## Capability Layers

### Platform Capabilities

- Electron 43 + Vue 桌面壳、本地 FastAPI、SQLite 和受控运行数据；
- Chrome CDP、任务实例、调度、文件/媒体、导出、通知与审计；
- Adapter 安装、版本信息与安装来源记录、任务参数、脚本执行和结果导出；
- AI/LLM/OCR Provider 路由、任务轮询、结果缓存和错误边界；
- 云端审批、RBAC、任务机 capability、租约、心跳、取消与幂等。

当前限制：企业业务逻辑仍有一部分位于 `core/` 专用处理器和 API 分支中，尚未全部隔离到 Adapter/配置；Manifest 权限声明也未形成主项目统一执行门禁。云端任务机授权和 Harness 会话审批是不同层面的机制。

### Reusable Ecommerce Capabilities

- 商品/订单/流量数据采集与导出；
- 企业网盘素材检索、下载、整理、上传和交付核验；
- AI 生图、生视频、图片编辑、OCR/PDF 与 Prompt 协同；
- 审核表、批次重跑、平台上传、发布和结果回收。

### Business Solutions

森马、巴拉、深绘、天猫、唯品会、TEMU 等具体流程属于方案层。凡是依赖企业目录、内部系统、固定字段、款号规则、SOP 或私有验收口径的能力，默认视为业务定制；只有抽象成稳定契约、移除私有凭证并补齐独立验收后，才进入可对外开放的公共能力。

## Product Principles

1. **业务结果优先**：完成定义是产物或目标系统状态可读回，不是按钮已点击或接口返回 200。
2. **本地优先、云端协作**：登录态、文件和高风险执行留在本地任务机；云端承载共享状态、审批和调度。
3. **显式能力边界**：Adapter 的文档与配置应说明实际使用的能力、外部依赖和支持环境，执行遵循平台授权策略。
4. **人在关键环节**：上传、发布、删除、付费或业务不可逆操作遵循审批与审计策略。
5. **失败可恢复**：长链路要有检查点、幂等键、重跑入口和清楚的下一步。
6. **固定流程与智能体并存**：高频稳定任务走确定性工作流；探索和跨工具编排可走 Harness，但最终仍落到受控能力。
7. **平台与定制分离**：企业规则放在 Adapter、模板和配置中，不污染公共底座。

## Brand Personality

务实、清晰、可信、利落。界面延续抓虾深色工作台和橙色品牌锚点，强调状态、任务和结果，不做独立 SaaS 营销页式装饰。

## Design Principles

1. 主任务与当前状态一眼可见，用户不需要理解内部技术模块。
2. 表格、图片、视频、日志和审批证据按任务上下文组织。
3. 待审批、运行中、可恢复、失败和完成必须有稳定且明确的表达。
4. 主项目嵌入云端页、Harness 桌面端嵌入 DSH 会话时，应像原生产品，不出现重复导航或割裂外壳。
5. 所有失败都给可行动的下一步；高风险动作说明影响范围并要求合适的确认。
6. 颜色不是唯一状态信号，关键操作支持键盘焦点并尊重 reduced motion。

## Out of Scope

- 把抓虾包装成无行业边界的通用 RPA 或低代码平台；
- 默认把企业私有 Adapter、目录规则或凭证公开分发；
- 让云端直接持有并任意使用本地浏览器登录态；
- 让智能体绕过 Adapter 权限、审批、审计或验收边界；
- 把尚未验证的模型、页面或内部系统能力写成稳定承诺。

更完整的系统边界、Harness 关系和开放策略见 `SPEC.md`。
